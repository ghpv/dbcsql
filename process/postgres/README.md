# Design By Contract SQL generator core library

## Testing

All tests assume that there is a `postgres` server running.

Sample docker compose configuration is available is `./docker/test-postgres`.
